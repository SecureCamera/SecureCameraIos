- when switching between photo and video modes, the zoom should reset back to the default 1.0x. Zoom should reset to
  1.0x when coming out of the background to camera mode.
- (bug) when flash is enabled, clicking to disable doesn't always toggle the button.
- (bug) swiping sideways to a video doesn't show the video. it just shows a spinner on a black screen. it should show
  the video. videos can't be viewed at all.

